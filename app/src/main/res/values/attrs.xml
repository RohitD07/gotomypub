<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="FontTextView">
        <attr name="customfont" />
    </declare-styleable>
    <declare-styleable name="FontButtton">
        <attr name="customfont" />
    </declare-styleable>

    <declare-styleable name="IconTextView">
        <attr name="iconcustomfont" />
    </declare-styleable>

    <attr name="iconcustomfont" format="enum">
        <enum name="ic_font_light" value="0"></enum>
        <enum name="ic_font_bold" value="1"></enum>
    </attr>

    <attr name="customfont" format="enum">
        <enum name="font_light" value="0"></enum>
        <enum name="font_bold" value="1"></enum>
    </attr>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

</resources>
